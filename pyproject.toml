[tool.poetry]
name = "bag"
version = "0.0.0"
description = ""
authors = []
maintainers = ["Bob Zhou <bob.linchuan@berkeley.edu>"]
readme = "README.md"
repository = "https://github.com/ucb-art/bag"
packages = [
    {include = "bag`"}
]

[tool.poetry.dependencies]
python = "^3.7"
PyYAML = "^6.0"
"ruamel.yaml" = "^0.15.87"
networkx = "^2.4"
numpy = "^1.21.2"

[tool.poetry.dev-dependencies]
pytest = "^7.1"
sphinx = "^5.0.2"
sphinx-rtd-theme = "^1.0.0"
sphinx-jsonschema = "^1.19.1"
sphinx_rtd_size = "^0.2.0"

[[tool.poetry.source]]
name = "testpypi"
url = "https://test.pypi.org/legacy/"
priority = "explicit"

[build-system]
requires = ["poetry-core>=1.0.8", "setuptools>=65.3"]
build-backend = "poetry.core.masonry.api"

[testenv]
deps = pytest
commands =
  pytest tests/ {posargs}
"""
